# kdbxPushToRemote configuration

sourceDir=..\databaseCopies
Remotes=Remote1,Remote2,Remote3
RemotePath=kdbx-backup

# RclonePath=null means "use rclone from PATH".
# Set to a full path if rclone is not on PATH, e.g. C:\Tools\rclone\rclone.exe

RclonePath=null
