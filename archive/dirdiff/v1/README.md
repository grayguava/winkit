# dirdiff (Python) — compare two directories

Superseded by `dirdiff/` (C# rewrite). This Python version used PowerShell to
invoke a Windows folder dialog and walked directories with `os.walk`. It required
Python 3.8+ and had no external dependencies.

Kept for reference.
